# API設定
# デフォルトでは、AWS API (https://zu9mkxoir4.execute-api.ap-northeast-1.amazonaws.com/api) への接続を試み、
# 失敗した場合は自動的にローカルAPI (http://localhost:8080/api) にフォールバックします

# 明示的にAPIエンドポイントを指定する場合（自動フォールバック無効）:
# VITE_API_BASE_URL=https://zu9mkxoir4.execute-api.ap-northeast-1.amazonaws.com/api

# ローカルAPIのエンドポイントを変更する場合:
# VITE_LOCAL_API_BASE_URL=http://localhost:8080/api
